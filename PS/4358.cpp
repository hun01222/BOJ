#include<iostream>
#include<map>
#include<string>
using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	string s;
	map<string, float> map;
	int cnt=0;
	while(getline(cin, s)){
		cnt++;
	}
}